1. test_camera.py: Capture facial images from the camera and save them.

2. face_training.py: Read the captured images, detect faces, extract features, and train the LBPH model.

3. face_recognition.py: Load the trained model to perform real-time facial recognition on camera footage.
